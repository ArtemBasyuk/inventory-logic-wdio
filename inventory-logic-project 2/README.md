# Inventory Logic Project

## Sorting Validation Logic
We collect all prices from the UI, convert them to numbers, and store them in an array.

Then we validate sorting using a helper function:
- Loop through array
- Compare current and next value
- If any current > next → array is not sorted

## Structure
- POM (Page Object Model)
- Data Provider for test data
- Logger utility
- Sorting helper
